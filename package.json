{
  "name": "corlized-logs",
  "version": "1.0.0",
  "description": "Nodejs console logs colorized",
  "main": "dist/index.js",
  "umd:main": "dist/index.js",
  "source": "src/index.js",
  "files": [
    "dist",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:types && node esbuild.build.js",
    "build:types": "tsc --declaration --outDir dist --allowSyntheticDefaultImports src/index.ts",
    "lint": "eslint src",
    "prepare": "npm run build",
    "prepublish": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jahirvidrio/colorized-logs.git"
  },
  "keywords": [
    "logger"
  ],
  "author": "Jahir Vidrio",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jahirvidrio/colorized-logs/issues"
  },
  "homepage": "https://github.com/jahirvidrio/colorized-logs#readme",
  "devDependencies": {
    "@types/node": "^15.12.2",
    "esbuild": "^0.12.8",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "colors": "^1.4.0"
  }
}
